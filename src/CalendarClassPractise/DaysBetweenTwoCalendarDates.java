package CalendarClassPractise;

public class DaysBetweenTwoCalendarDates {
	/*
	 * 
	 * private void onDateSet(){ Calendar endDate = Calendar.getInstance(),startDate
	 * = Calendar.getInstance(); int currentMonthOfYear; int currentYear; int
	 * currentDayOfMonth;
	 * startDate.set(currentYear,currentMonthOfYear,currentDayOfMonth);
	 * endDate.set(currentYear,currentMonthOfYear,currentDayOfMonth);
	 * datesToAdd(startDate,endDate); }
	 * 
	 * //call for get dates list private List<Date> datesToAdd(Calendar
	 * startDate,Calendar endDate){ List<Dates> datesLists = new List<>(); while
	 * (startDate.get(Calendar.YEAR) != endDate.get(Calendar.YEAR) ||
	 * startDate.get(Calendar.MONTH) != endDate.get(Calendar.MONTH) ||
	 * startDate.get(Calendar.DAY_OF_MONTH) != endDate.get(Calendar.DAY_OF_MONTH)) {
	 * 
	 * datesList.add(new Date(startDate.get(Calendar.YEAR),
	 * startDate.get(Calendar.MONTH), startDate.get(Calendar.DATE));
	 * 
	 * startDate.add(Calendar.DATE, 1);//increas dates
	 * 
	 * } return datesList;
	 */

	public static void main(String[] args) {

	}

}
